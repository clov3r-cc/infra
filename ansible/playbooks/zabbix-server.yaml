---
- name: Configure Zabbix servers
  hosts: zabbix_server, zabbix_server__qdevice
  become: true
  gather_facts: true
  tags: [zabbix-server, zabbix, ha]
  roles:
    # - role: common
    #   tags: [common]
    # - role: ondrejhome.pcs-modules-2
    #   tags: [pcs-modules, ha]
    - role: zabbix_server
      tags: [zabbix, ha, drbd, pacemaker]
