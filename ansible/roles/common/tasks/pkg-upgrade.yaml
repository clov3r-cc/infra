---
- name: pkg-upgrade | Setup automatic upgrade (RedHat)
  when: ansible_facts['os_family'] == 'RedHat'
  block:
    - name: pkg-upgrade | Install dnf-automatic
      ansible.builtin.package:
        name: dnf-automatic
        state: present

    - name: pkg-upgrade | Configure dnf-automatic to apply upgrades
      community.general.ini_file:
        path: /etc/dnf/automatic.conf
        section: commands
        option: apply_updates
        value: "yes"
        mode: "0644"

    - name: pkg-upgrade | Configure dnf-automatic to reboot when needed
      community.general.ini_file:
        path: /etc/dnf/automatic.conf
        section: reboot
        option: reboot
        value: when-needed
        mode: "0644"

    - name: pkg-upgrade | Deploy dnf-automatic timer
      ansible.builtin.copy:
        dest: /etc/systemd/system/dnf-automatic.timer
        owner: root
        group: root
        mode: "0644"
        content: |
          [Unit]
          Description=dnf-automatic timer

          [Timer]
          OnCalendar={{ pkg_upgrade_calendar }}
          RandomizedDelaySec=0
          Persistent=true

          [Install]
          WantedBy=timers.target

    - name: pkg-upgrade | Enable and start dnf-automatic timer
      ansible.builtin.systemd_service:
        name: dnf-automatic.timer
        daemon_reload: true
        enabled: true
        state: started

- name: pkg-upgrade | Setup automatic upgrade (Debian)
  when: ansible_facts['os_family'] == 'Debian'
  block:
    - name: pkg-upgrade | Install unattended-upgrades
      ansible.builtin.package:
        name: unattended-upgrades
        state: present

    - name: pkg-upgrade | Enable unattended-upgrades
      ansible.builtin.debconf:
        name: unattended-upgrades
        question: unattended-upgrades/enable_auto_updates
        value: "true"
        vtype: boolean

    - name: pkg-upgrade | Configure apt::periodic for weekly upgrades
      ansible.builtin.copy:
        dest: /etc/apt/apt.conf.d/20auto-upgrades
        owner: root
        group: root
        mode: "0644"
        content: |
          APT::Periodic::Update-Package-Lists "1";
          APT::Periodic::Unattended-Upgrade "1";

    - name: pkg-upgrade | Deploy systemd override for unattended-upgrades timer
      ansible.builtin.file:
        path: /etc/systemd/system/apt-daily-upgrade.timer.d
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: pkg-upgrade | Override apt-daily-upgrade timer schedule
      ansible.builtin.copy:
        dest: /etc/systemd/system/apt-daily-upgrade.timer.d/override.conf
        owner: root
        group: root
        mode: "0644"
        content: |
          [Timer]
          # 既存のスケジュールをリセット
          OnCalendar=
          OnCalendar={{ pkg_upgrade_calendar }}
          RandomizedDelaySec=0

    - name: pkg-upgrade | Configure unattended-upgrades to reboot when needed
      ansible.builtin.copy:
        dest: /etc/apt/apt.conf.d/99auto-reboot
        owner: root
        group: root
        mode: "0644"
        content: |
          Unattended-Upgrade::Automatic-Reboot "true";
          Unattended-Upgrade::Automatic-Reboot-WithUsers "false";

    - name: pkg-upgrade | Enable and start apt-daily-upgrade timer
      ansible.builtin.systemd_service:
        name: apt-daily-upgrade.timer
        daemon_reload: true
        enabled: true
        state: started
