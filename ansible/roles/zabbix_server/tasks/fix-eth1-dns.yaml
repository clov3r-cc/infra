---
- name: fix-eth1-dns | Fix eth1 DNS contamination
  when: ansible_facts['os_family'] == 'RedHat'
  community.general.nmcli:
    conn_name: "cloud-init eth1"
    type: ethernet
    dns4: []
    dns4_ignore_auto: true
    state: present
  notify: Reload NetworkManager connection eth1
